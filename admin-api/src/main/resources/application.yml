core-client:
  base-url: ${CORE_BASE_URL:http://localhost:5000}
  connect-timeout: 5000
server:
  port: ${ADMIN_API_PORT:5001}
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
